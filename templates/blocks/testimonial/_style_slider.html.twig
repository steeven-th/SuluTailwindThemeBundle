{# Testimonial block — carousel slider #}
{% extends '@ItechWorldSuluTailwindTheme/blocks/common/_block_wrapper.html.twig' %}

{% block block_content %}
    {% include '@ItechWorldSuluTailwindTheme/blocks/common/_titles.html.twig' with {
        title: title|default(''),
        subTitle: subTitle|default(''),
        titleAlignment: titleAlignment|default('left'),
        variant: variant|default(0)
    } only %}

    {% if testimonials is defined and testimonials is iterable %}
        <div
            data-controller="slider"
            data-slider-mode-value="carousel"
            data-slider-equal-height-value="true"
            data-slider-autoplay-value="true"
            data-slider-interval-value="6000"
            class="relative"
            style="--iw-gallery-nav-color: currentColor; --iw-gallery-nav-bg: color-mix(in srgb, currentColor 8%, transparent); --iw-gallery-nav-bg-hover: color-mix(in srgb, currentColor 15%, transparent);"
        >
            {# Grid stack: all slides in the same cell, container takes tallest height #}
            <div class="grid px-14">
                {% for item in testimonials %}
                    {% if item.type|default('testimonial') == 'testimonial' %}
                        <div data-slider-target="slide"
                             style="grid-area: 1/1;"
                             class="text-center {{ paragraphImageRadius|default('') }} {{ loop.first ? '' : 'invisible pointer-events-none' }}">
                            {# Large quote #}
                            <svg class="w-12 h-12 text-[var(--color-primary)] opacity-20 mx-auto mb-6" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10H14.017zM0 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151C7.546 6.068 5.983 8.789 5.983 11h4v10H0z"/>
                            </svg>

                            {% if item.quote is defined and item.quote is not empty %}
                                <p class="text-xl md:text-2xl italic leading-relaxed">{{ item.quote }}</p>
                            {% endif %}

                            <div class="flex items-center justify-center gap-3 mt-6">
                                {% if item.avatar is defined and item.avatar.id is defined %}
                                    {% set avatarMedia = sulu_resolve_media(item.avatar.id, app.request.locale) %}
                                    {% include '@ItechWorldSuluTailwindTheme/blocks/common/_avatar.html.twig' with {
                                        avatar: avatarMedia,
                                        name: item.author|default(''),
                                        size: 'lg'
                                    } only %}
                                {% endif %}
                                <div class="text-left">
                                    {% if item.author is defined and item.author is not empty %}
                                        <span class="block font-semibold">{{ item.author }}</span>
                                    {% endif %}
                                    {% if item.role is defined and item.role is not empty %}
                                        <span class="block text-sm opacity-60">{{ item.role }}</span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    {% endif %}
                {% endfor %}
            </div>

            {# Navigation arrows — same style as gallery sliders #}
            <button
                type="button"
                data-action="slider#prev"
                class="iw-gallery-nav absolute left-0 top-1/2 -translate-y-1/2 z-10"
                aria-label="Previous"
            >
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
            </button>
            <button
                type="button"
                data-action="slider#next"
                class="iw-gallery-nav absolute right-0 top-1/2 -translate-y-1/2 z-10"
                aria-label="Next"
            >
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
            </button>

            {# Dots — adapts to variant via currentColor #}
            <div data-slider-target="dots" class="flex items-center justify-center gap-2 mt-6">
                {% for item in testimonials %}
                    {% if item.type|default('testimonial') == 'testimonial' %}
                        <button
                            type="button"
                            data-action="slider#goTo"
                            data-slider-index-param="{{ loop.index0 }}"
                            class="w-2.5 h-2.5 rounded-full bg-current transition-opacity {{ loop.first ? '' : 'opacity-30' }}"
                            aria-label="Slide {{ loop.index }}"
                        ></button>
                    {% endif %}
                {% endfor %}
            </div>
        </div>
    {% endif %}
{% endblock %}
